<UserControl
    x:Class="SHControls.Views.DevicesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SHControls.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:switches ="using:SwitchesControls.Views"   
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    IsEnabled="{Binding Path=IsEnabled}">
    
    <Grid>
        <ProgressRing IsActive="{Binding Path=PBIsActive}" Height="100" Width="100" Foreground="#FFBBBBBB"/>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition />
            </Grid.RowDefinitions>

            <CommandBar OverflowButtonVisibility="Collapsed" Background="Transparent">
                <AppBarButton Icon="Find" Command="{Binding FindDevices}"/>
                <AppBarButton Icon="Refresh" Command="{Binding Refresh}"/>
            </CommandBar>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" VerticalScrollMode="Auto">
                <StackPanel Orientation="Vertical">
                    <switches:SwitchesView DataContext="{Binding Path=Switches}"/>
                </StackPanel>
            </ScrollViewer>

        </Grid>
    </Grid>
</UserControl>
