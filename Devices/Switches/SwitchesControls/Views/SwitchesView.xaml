<UserControl
    x:Class="SwitchesControls.Views.SwitchesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SwitchesControls.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:swVM="using:SwitchesControls.ViewModels"
    xmlns:shCtrls ="using:SHBaseControls"    
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/StylesOfSwitches.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <!--<ListView ItemsSource="{Binding Path=Switches.List}" ItemTemplate="{StaticResource SwitchOutletItemTemplate}"/>

        <ListView Grid.Row="1" ItemsSource="{Binding Path=Outlets.List}" ItemTemplate="{StaticResource SwitchOutletItemTemplate}"/>-->

        <shCtrls:DevicesGroup ItemsSource="{Binding Path=Switches.List}" ItemTemplate="{StaticResource SwitchOutletItemTemplate}"/>

    </Grid>
</UserControl>
