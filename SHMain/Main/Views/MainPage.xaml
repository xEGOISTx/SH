<Page
    x:Class="SHMain.Main.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SHMain"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dPresenter="using:DevicesPresenterControls.Views"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Name="thisControl">
    

    <Grid>
        <dPresenter:DevicePresenterView DataContext="{Binding Path=DevicePresenterVM}"/>
        <!--<ProgressRing IsActive="{Binding Path=PBIsActive}" Height="100" Width="100"/>-->

        <!--<dPresenter:DevicePresenterView DataContext="{Binding Path=DevicePresenterVM}"
                                        Visibility="{Binding ElementName=thisControl, Path=DataContext.DevicePresenterVisibility}"/>-->
    </Grid>
</Page>
